---
name: Bug report
about: Something doesn't work the way the tool description says it should
title: ""
labels: bug
---

**Tool(s) involved**
e.g. `add_node`, `run_scene`, the WebSocket bridge itself

**What happened**
What you called it with, what you expected, what you actually got (paste the
tool's JSON response if there is one).

**Steps to reproduce**
1.
2.
3.

**Environment**
- Godot version:
- OS:
- AI client (Claude Desktop / Cursor / Claude Code / other):
- godot-mcp-bridge version (`get_runtime_status` or `npx godot-mcp-bridge doctor`):

**Logs**
Anything from `get_console_log` / `get_errors`, or the MCP server's stdout, that looks relevant.
